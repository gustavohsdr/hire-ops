{
  "name": "rh",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run prisma:studio --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
